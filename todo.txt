
Søndag d. 23.:

1) gør så man kun kan access den hvis man er logget ind ✓
2) udvælg hvilke bonus emner jeg vil skrive mere om 
3) ryd op i kode ✓
4) ryd op i css ✓
5) lav ny branch til hosting ✓
6) opret hosting evt. på Heroku ✓ 
7) del heroku-link 

- gjort så admin-linket i header redirecter til /admin hvis man allerede er logget ind ✓


Bonus emner:
- type coersion 
- referential transparency 
- hoisting
- Clean code / code conventions.  Can argue why clean code and following code conventions is a good idea.
- Sending data with GET requests


fremtidige features:
- log ud knap 
- bedre footer med mere info 
- at admin siden rent faktisk gemmer det der sendes 
- at session siderne er én side der ændres fra gang til gang 


Lørdag d. 22:

1) gennemgå emner med August og få styr på dem ✓
2) renskriv noterne ✓
3) lav en side hvor man kan skrive ting, hvis man er logget ind ✓
4) gør så man kan tilføje billeder  ✓
5) tilføj terminal eksempler i noterne ✓
6) tilføj kodeeksempler i noterne ✓


inden aflevering:
1) ryd op i koden ✓
2) ryd særligt op i css fil! ✓
2) læg den online evt. på heroku ✓


1) læs krav ✓
2) login funktion? hardcoded? evt via et array med users? ✓
3) renskriv noter ✓
4) læg noter op til de tilsvarende sider ✓
5) lav funktion til at tilføje noget
6) research de emner jeg ikke har noter om og udfyld de emner ✓
7) overvej hvilke af de ekstra emner der er værd at skrive om 
8) find eller lav code snippets til dele af koden (Har masser i mine noter) ✓
9) det samme med terminal commands ✓


Requirements for the project:

- Serve the pages with Express. ✓
- Use Server-Side Rendering (SSR). ✓
- Split up the documentation in pages rather than one long HTML page. ✓
- Should have some nice styling (custom or with a styling framework). ✓

Requirements about the documentation:

- Should contain terminal commands. ✓
- Should contain code snippets. ✓
- Should contain all the tools we’ve used and explain them. ✓
- Should cover all the theory that we’ve touched upon. ✓

Admin panel:

- Create (a) login/signup page(s). ✓
- The admin panel should allow users to fill out the content for a new documentation page in the browser. ✓
- Bonus (Which means that this is not a requirement): Allow users to create new pages. 

Bonus:

- Research a covered topic in-depth that you want to understand better.
- Learn something new and document it.
